model_name: SurgiSAM2
training_state: frozen

sam2_repo_root: "F:/Models/SAM2"
surgisam2_repo_root: "F:/Models/SurgiSAM2"

checkpoint: "F:/Models/SurgiSAM2/checkpoints/Curated400_checkpoint.pt"

# SurgiSAM2 paper/repo says the fine-tuned model is based on SAM2 Base Plus.
model_cfg: "configs/sam2/sam2_hiera_b+.yaml"

device: "cuda"
multimask_output: true
mask_selection: "highest_score"
use_bfloat16: true