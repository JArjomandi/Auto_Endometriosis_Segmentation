experiment_name: "ENID_DeepLabV3Plus_Trained"

dataset_config: "configs/datasets/enid.yaml"
model_config: "configs/models/deeplabv3plus.yaml"

output_root: "F:/Results/SAM_Benchmarking/ENID/DeepLabV3Plus/trained"

splits:
  train: train
  val: val
  test: test

save:
  merged_masks: true
  overlays: true