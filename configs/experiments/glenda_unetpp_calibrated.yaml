experiment_name: "GLENDA_UNetPP_Calibrated"

dataset_config: "configs/datasets/glenda.yaml"
model_config: "configs/models/unetpp_calibrated.yaml"

output_root: "F:/Results/SAM_Benchmarking/GLENDA/UNetPP_calibrated/trained"

splits:
  train: "train"
  val: "val"
  test: "test"

save:
  merged_masks: true
  overlays: true
  checkpoints: true