experiment_name: "ENID_SegFormer_Trained"
dataset_config: "configs/datasets/enid.yaml"
model_config: "configs/models/segformer.yaml"
output_root: "F:/Results/SAM_Benchmarking/ENID/SegFormer/trained"

splits:
  train: train
  val: val
  test: test

save:
  merged_masks: true
  overlays: true