experiment_name: "GLENDA_clean_SegFormer_Trained"
dataset_config: "configs/datasets/glenda_clean.yaml"
model_config: "configs/models/segformer.yaml"
output_root: "F:/Results/SAM_Benchmarking/GLENDA_clean/SegFormer/trained"

splits:
  train: train
  val: val
  test: test

save:
  merged_masks: true
  overlays: true