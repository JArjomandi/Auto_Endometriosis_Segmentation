experiment_name: "ENID_UNetPP_Calibrated"

dataset_config: "configs/datasets/enid.yaml"
model_config: "configs/models/unetpp_calibrated.yaml"

output_root: "F:/Results/SAM_Benchmarking/ENID/UNetPP_calibrated/trained"

splits:
  train: "train"
  val: "val"
  test: "test"

save:
  merged_masks: true
  overlays: true
  checkpoints: true