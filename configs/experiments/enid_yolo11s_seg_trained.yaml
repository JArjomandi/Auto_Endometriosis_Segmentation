experiment_name: "ENID_YOLO11s_seg_Trained"

dataset_config: "configs/datasets/enid.yaml"
model_config: "configs/models/yolo11s_seg.yaml"

yolo_dataset_yaml: "F:/Datasets/YOLO datasets/ENID_yolo_seg/enid_yolo_seg.yaml"

output_root: "F:/Results/SAM_Benchmarking/ENID/YOLO11s_seg/trained"

splits:
  train: train
  val: val
  test: test

save:
  merged_masks: true
  overlays: true
  auto_box_prompts: true