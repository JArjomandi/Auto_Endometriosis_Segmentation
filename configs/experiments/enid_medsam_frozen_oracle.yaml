experiment_name: "ENID_MedSAM_Frozen_OracleBox"

dataset_config: "configs/datasets/enid.yaml"
model_config: "configs/models/medsam_frozen.yaml"

output_root: "F:/Results/SAM_Benchmarking/ENID/MedSAM/frozen"

splits:
  - val
  - test

prompt_modes:
  - GT_box

save:
  instance_masks: true
  merged_masks: true
  overlays: true