experiment_name: "GLENDA_SegFormer_Calibrated"
dataset_config: "configs/datasets/glenda.yaml"
model_config: "configs/models/segformer_calibrated.yaml"
output_root: "F:/Results/SAM_Benchmarking/GLENDA/SegFormer_calibrated/trained"

splits:
  train: train
  val: val
  test: test

save:
  merged_masks: true
  overlays: true