experiment_name: "GLENDA_clean_DeepLabV3Plus_Trained"

dataset_config: "configs/datasets/glenda_clean.yaml"
model_config: "configs/models/deeplabv3plus.yaml"

output_root: "F:/Results/SAM_Benchmarking/GLENDA_clean/DeepLabV3Plus/trained"

splits:
  train: train
  val: val
  test: test

save:
  merged_masks: true
  overlays: true