experiment_name: "GLENDA_clean_UNetPP_Trained"

dataset_config: "configs/datasets/glenda_clean.yaml"
model_config: "configs/models/unetpp.yaml"

output_root: "F:/Results/SAM_Benchmarking/GLENDA_clean/UNetPP/trained"

splits:
  train: "train"
  val: "val"
  test: "test"

save:
  merged_masks: true
  overlays: true
  checkpoints: true