experiment_name: "GLENDA_YOLO11s_seg_Trained"

dataset_config: "configs/datasets/glenda.yaml"
model_config: "configs/models/yolo11s_seg.yaml"

yolo_dataset_yaml: "F:/Datasets/YOLO datasets/GLENDA_yolo_seg/glenda_yolo_seg.yaml"

output_root: "F:/Results/SAM_Benchmarking/GLENDA/YOLO11s_seg/trained"

splits:
  train: train
  val: val
  test: test

save:
  merged_masks: true
  overlays: true
  auto_box_prompts: true