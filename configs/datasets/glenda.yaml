dataset_name: GLENDA
dataset_root: "F:/Datasets/Standardized datasets/GLENDA/GLENDA 60_20_20 split"

splits:
  train:
    images: "train/images"
    masks: "train/masks"
    prompts: "oracle_prompts/GLENDA_train_oracle_prompts.csv"

  val:
    images: "val/images"
    masks: "val/masks"
    prompts: "oracle_prompts/GLENDA_val_oracle_prompts.csv"

  test:
    images: "test/images"
    masks: "test/masks"
    prompts: "oracle_prompts/GLENDA_test_oracle_prompts.csv"